# 小黑盒填入助手

这是 AI 新闻工作台面向常用 Google Chrome 的发布适配器，不依赖 Codex 或电脑操作 Harness。

本地开发安装：

1. 在 Chrome 打开 `chrome://extensions`。
2. 打开右上角“开发者模式”。
3. 点击“加载已解压的扩展程序”。
4. 选择当前 `chrome-extension` 文件夹。
5. 刷新 `http://127.0.0.1:4317/`。

扩展只访问本地工作台和小黑盒域名，只填入编辑器，不会点击最终发布。
